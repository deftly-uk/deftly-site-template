# Dependencies
node_modules/

# Next.js
.next/
out/
build/
next-env.d.ts

# Vercel
.vercel/

# Environment — never commit secrets
.env
.env.*
!.env.example

# Images — live in Vercel Blob, never in git (Constitution Article III)
public/uploads/
raw-images/
seed-cache/
# Local-disk upload output (when no Blob token is set, e.g. dev/tests)
/media/

# Test artifacts
coverage/

# Old scaffold, kept locally for reference only
_graveyard/

# Misc
.DS_Store
*.tsbuildinfo
*.log
npm-debug.log*
