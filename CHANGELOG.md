# Changelog

All notable changes to the Deftly Site Template are documented in this file.

## [Unreleased]

### Documentation
- Added a status banner to `README.md` and a new `CLAUDE.md`: this is an unstyled, never-deployed scaffold — the #1 blocker for the site-generation engine — pointing readers and Claude sessions at the cross-repo master plan as the source of truth.
- Corrected the `gh repo create` template reference from the old `hm293` account to the `deftly-uk` org.
