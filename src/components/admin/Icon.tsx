import React from 'react'

/** Small mark shown in the admin nav (Constitution Article IV). */
export const Icon: React.FC = () => (
  <span
    style={{
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: 28,
      height: 28,
      borderRadius: 7,
      background: '#173A5E',
      color: '#fff',
      fontWeight: 700,
      fontSize: 16,
    }}
  >
    D
  </span>
)

export default Icon
